# Deploy to Quave Cloud staging — wired in roadmap issue #12.
# Placeholder only: do not enable until Dockerfile + secrets are ready.
#
# name: Deploy staging
# on:
#   push:
#     branches: [main]
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: pnpm/action-setup@v4
#         with:
#           version: 10
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 24
#           cache: pnpm
#       - run: pnpm install --frozen-lockfile
#       - run: pnpm build
#       - uses: zcloud-ws/zcloud-deploy-action@main
#         with:
#           token: ${{ secrets.QUAVE_CLOUD_ENV_TOKEN }}
#           # env: joaovictornsv-ship-it-staging

name: Deploy staging (stub)

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Not wired yet
        run: echo "Deploy workflow lands in issue #12 (Quave staging)."
