/** Static contributor skins — filled in contributor skins issue. */
export const contributors = [] as const;

/** Re-export talk name-drops until skins populate display names (#10 / #31). */
export { TALK_CONTRIBUTOR_NAMES } from './talkNames';
