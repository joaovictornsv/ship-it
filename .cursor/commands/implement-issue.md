# Implement issue — stub

Follow `.cursor/skills/implement-issue/SKILL.md`.

**Before anything else:** sync to latest `main` (`git checkout main && git pull`). If the working tree has uncommitted changes, stop and ask the user how to proceed — do not stash, discard, or commit unless they say so.

Player-facing UI must follow `docs/modules/ui.md` (checklist includes a **(UI)** item).
