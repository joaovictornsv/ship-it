# Open a PR

Follow `.cursor/skills/create-pr/SKILL.md`.

Run `check-quality` (verify required; review/audit per reference) before
`gh pr create`. Agent owns and checks off the **Agent test plan**; leave
**Human test plan** unchecked.
